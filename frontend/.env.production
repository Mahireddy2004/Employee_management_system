# Production environment configuration
# In production, either use the deployed backend domain (e.g. https://api.yourdomain.com/api)
# or relative /api if served behind a reverse proxy or same-domain deployment.
VITE_API_BASE_URL=/api
